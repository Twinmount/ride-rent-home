import LottieLoader from "./LottieLoader";

const AnimatedSkelton = () => {
  return (
    <section className="flex h-screen flex-col items-center justify-center">
      <LottieLoader text="Searching for your results..." />
    </section>
  );
};
export default AnimatedSkelton;
